---
import Header from '../components/Header.astro';
import PageHero from '../components/PageHero.astro';
import PranikYorum from '../components/PranikYorum.astro';
import Subscribe from '../components/Subscribe.astro';
import siteContent, { pranikYorum } from '../data/site-content';
import BaseLayout from '../layouts/BaseLayout.astro';

const { title, desc, navTitle, navText } = siteContent.pranikSifaYorumlari;
---

<BaseLayout title={title} description={desc}>
    <div class="relative border-b border-slate-100 bg-slate-50" slot="header">
        <Header class="relative" />
        <PageHero {...siteContent.pranikSifaYorumlari.hero} />
    </div>
    <section class="grow px-4 py-12 sm:px-8 sm:py-8">
        <Subscribe title={navTitle} text={navText} pranikYorum={true} />
    </section>
    <main class="grow px-4 py-12 sm:px-8 sm:py-16">
        <h1 class="sr-only">Blog</h1>
        <div class="mx-auto max-w-3xl xl:max-w-6xl">
            {pranikYorum.map((post) => <PranikYorum yorum={post} />)}
        </div>
    </main>
    <section class="grow px-4 py-12 sm:px-8 sm:py-8">
        <Subscribe title={navTitle} text={navText} pranikYorum={true} />
    </section>
</BaseLayout>
